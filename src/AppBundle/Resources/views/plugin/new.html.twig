{% extends 'base.html.twig' %}

{% block title %}New Plugin{% endblock %}


{% block body %}
    <h1>New Plugin</h1>

    {{ form_start(form) }}

    <div class="form-group">
        {{ form_label(form.jarFile) }}
        <div class="col-sm-10">
            <div class="input-group">
                <label class="input-group-btn">
                    <span class="btn btn-primary">
                        Browse… {{ form_widget(form.jarFile, {'attr': {'class': 'hidden'}}) }} 
                    </span>
                </label>                    
                <input type="text" class="form-control" readonly="" id="filename">
            </div>
            {% if form.jarFile.vars.attr.help_block %}
                <p class='help-block'>
                    {{ form.jarFile.vars.attr.help_block }}
                </p>
            {% endif %}
        </div>
    </div>
    
    
    {{ form_rest(form) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Create" class="btn btn-primary" />
            <a href="{{ path('plugin_index') }}" class="btn btn-warning">Cancel</a>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}
