Please do not reply. This is an automatically generated email
notification.

Your LOCKSS box {{ box.getHostname() }} ( {{ box.getIpAddress() }}) is running low
on disk space. When LOCKSSOMatic checked on the box at {{ boxStatus.created() | date }}
it reported the disk space remaining as follows:
{% for data in boxStatus.data %}
    Cache ID: {{ data.repositorySpaceId }}
    Percentage Full: {{ data.percentageFull * 100 }}%
    Disk Size: {% embed 'partial/size.html.twig' with {bytes: data.size} %}{% endembed %}
    Free Space: {% embed 'partial/size.html.twig' with {bytes: data.size} %}{% endembed %}
    Used Space: {% embed 'partial/size.html.twig' with {bytes: data.size} %}{% endembed %}
{% endfor %}
If you need assistance or have questions about this email, please
contact {{ contact }}.
